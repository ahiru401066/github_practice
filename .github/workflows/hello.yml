name: hello world

on:
    push:

jobs:
    say-hello:
        runs-on: ubuntu-latest

        steps:
            - name: チェックアウト
              uses: actions/checkout@v4
            - name: Helloを表示
              run: echo "Hello, Github Actions!"